# Basic Compartment Model

Let see how to perform model generation using 'cmtool-data' workspace.
One can build its own model above, low-level files can be written with 'rawdata', namely *RawDataFlux* and *RawDataScalar*.
